#include "replay-manager.h"

/**
 * Manages the actual replays, which consist of timestamps, source #s (1-8), workspace #s (1-20),
 * and any other replay properties to be aware of (e.g. playback speed).
 */
