#pragma once

// Editor State
/**
 * Tracks what frame/source the editor is on, including forward playback, scrubs, and frame-by-
 * frame control. Compares against the timeline to avoid speeding/lagging, same as Preview State.
 */
